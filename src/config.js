const config = {
    defaultPath: '/',
    fontFamily: `'Roboto', sans-serif`, // Changed to Roboto
    i18n: 'en',
    miniDrawer: false,
    container: true,
    mode: 'light',
    presetColor: 'default',
    themeDirection: 'ltr'
}

export default config;
